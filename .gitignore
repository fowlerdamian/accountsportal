.vercel
supabase.exe
node_modules
dist
.mcp.json

# Env files — never commit any variant
.env
.env.*
!.env.example

# Secrets / keys
tools/.sldprt-key.bin

# Local tooling state (per-machine, not for the repo)
.claude/
supabase/.temp/
tools/logs/
